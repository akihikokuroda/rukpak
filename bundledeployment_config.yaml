apiVersion: core.rukpak.io/v1alpha1
kind: BundleDeployment
metadata:
  name: helm-proto1
spec:
  provisionerClassName: core.rukpak.io/plain
  config:
    values:
      key1: "value1"
      key2: "value2"
  template:
    metadata: {}
    spec:
      provisionerClassName: core.rukpak.io/plain
      source:
        local:
          configMap:
            name: rukpakctl-bd-s7gpk
            namespace: rukpak-system
        type: local

